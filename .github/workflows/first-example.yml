name: My First Workflow

on:push

jobs:
    first_job:
        runs-on: self-hosted
        steps:
        - name: Welcome message
          run: echo "my first github action job"

        - name: list files
          run: ls

        - name: read file
          run: cat README.md
        
         